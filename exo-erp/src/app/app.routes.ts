import { Routes } from '@angular/router';
import { InvoicePageComponent } from './features/invoice-page/invoice-page.component';

export const routes: Routes = [
    {
        path: '',
        component: InvoicePageComponent,
    },
];